{
  "name": "normcore",
  "version": "1.0.3",
  "description": "No-config distributed streams using hypercore",
  "main": "index.js",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --extra --no-dev",
    "test": "standard && npm run deps",
    "test:cov": "standard && npm run deps"
  },
  "repository": "yoshuawuyts/normcore",
  "keywords": [
    "hypercore",
    "for",
    "normies"
  ],
  "license": "MIT",
  "dependencies": {
    "hypercore": "^4.11.0",
    "hyperdrive-archive-swarm": "^5.0.2",
    "named-level-store": "^1.0.0"
  },
  "devDependencies": {
    "dependency-check": "^2.6.0",
    "istanbul": "^0.4.5",
    "standard": "^8.5.0",
    "tape": "^4.6.2"
  }
}
